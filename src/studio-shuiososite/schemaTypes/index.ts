import event from './event'
import bio from './bio'
import {eventFlyer} from "./eventFlyer";
import {galleryItem} from "./galleryItem";

export const schemaTypes = [eventFlyer, galleryItem];


export const schemaTypes = [event, bio]